"use client";

import type { UseQueueResult } from "../../hooks/useQueue";

// Placeholder - fleshed out fully in T31 (countdown, vibration, animation).
export function ConfirmTurn({ queue: _queue }: { queue: UseQueueResult }) {
  return <p className="text-cream-300">É a sua vez...</p>;
}
