"use client";

import type { UseQueueResult } from "../../hooks/useQueue";

// Placeholder - fleshed out fully in T30.
export function Waiting({ queue: _queue }: { queue: UseQueueResult }) {
  return <p className="text-cream-300">Você está na fila...</p>;
}
