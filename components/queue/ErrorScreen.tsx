"use client";

import type { UseQueueResult } from "../../hooks/useQueue";

// Placeholder - fleshed out fully in T33.
export function ErrorScreen({ queue: _queue }: { queue: UseQueueResult }) {
  return <p className="text-cream-300">Sem conexão...</p>;
}
